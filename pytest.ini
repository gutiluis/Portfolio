# add app package into sys.path for same working directory
# tells pytest (7+) to prepend the project root (where pytest.ini lives) to sys.path, so app imports resolve.
# python needs to import modules from the root of the project
[pytest]
pythonpath = .
